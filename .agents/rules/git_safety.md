# Git Safety Protocol

You MUST NEVER run `git commit` or `git push` autonomously.

Before staging or committing any code:
1. You MUST list out the exact files that you intend to commit.
2. You MUST wait for explicit user permission to proceed. 

Failure to follow this protocol risks unauthorized code modifications and is a strict violation of operational security.
